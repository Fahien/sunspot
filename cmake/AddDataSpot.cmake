# Add DataSpot
set(DATASPOT dataspot)
set(DATASPOT_INCLUDE_DIR ${MODULE_DIR}/${DATASPOT}/include)
set(DATASPOT_LIBRARY ${DATASPOT})

set(DATASPOT_INCLUDE_DIRS ${DATASPOT_INCLUDE_DIR})
set(DATASPOT_LIBRARIES ${DATASPOT_LIBRARY})

# Check DataSpot
if(NOT TARGET ${DATASPOT})
	add_subdirectory(${MODULE_DIR}/${DATASPOT})
endif()
